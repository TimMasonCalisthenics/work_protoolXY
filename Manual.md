# คู่มือการใช้งานระบบ (User Manual)

ยินดีต้อนรับสู่ระบบจัดการและตรวจสอบคุณภาพสินค้า (Quality Check & Measurement System) คู่มือฉบับนี้จะอธิบายวิธีการใช้งานหน้าต่างๆ ภายในระบบ โดยแบ่งตามสิทธิ์การใช้งาน (Roles) ซึ่งได้แก่ Admin, Supervisor และ Operator

---

## 1. การเข้าสู่ระบบ (Login)
- **หน้า Login**: ผู้ใช้ต้องกรอก Username และ Password เพื่อเข้าสู่ระบบ
- ระบบจะทำการตรวจสอบสิทธิ์ (Role) และนำทางไปยังหน้าแรกที่เหมาะสมกับสิทธิ์โดยอัตโนมัติ:
  - **Admin**: ไปที่หน้าจัดการผู้ใช้งาน (Manager User)
  - **Supervisor**: ไปที่หน้าจัดการสินค้า (Product Management)
  - **Operator**: ไปที่หน้าประวัติการวัด (Measurement History)

---

## 2. การจัดการผู้ใช้งาน (Manager User)
*สิทธิ์การเข้าถึง: Admin*
หน้านี้ใช้สำหรับจัดการบัญชีผู้ใช้ในระบบ
- **เพิ่มผู้ใช้ใหม่**: คลิกปุ่ม "Add New User" กรอก Username และ Password
- **ค้นหาผู้ใช้**: ใช้ช่องค้นหาเพื่อค้นหาจาก Username
- **แก้ไขสิทธิ์ (Role)**: คลิกลูกศร/ไอคอนดินสอ เพื่อเปลี่ยนสิทธิ์ของผู้ใช้งานเป็น Admin, Supervisor หรือ Operator
- **ลบผู้ใช้**: คลิกไอคอนถังขยะ และกด Confirm เพื่อลบผู้ใช้ออกจากระบบ

---

## 3. การจัดการสินค้า (Product Management)
*สิทธิ์การเข้าถึง: Admin, Supervisor*
หน้านี้ใช้สำหรับเพิ่ม แก้ไข ลบ และกำหนดสินค้าหลักที่กำลังเปิดใช้งาน (Active Product)
- **เพิ่ม/แก้ไขสินค้า**: คลิกปุ่ม "Add New Product" หรือไอคอนดินสอที่สินค้าเดิม
  - **Basic Info**: กรอกชื่อสินค้า และอัปโหลดรูปภาพรวมของสินค้า
  - **Specification Points**: เพิ่มจุดที่ต้องตรวจสอบคุณภาพ (Spec Points) โดยระบุชื่อจุด, ประเภทเซนเซอร์ที่ใช้ (Mitutoyo หรือ Air Gauge), ค่า Nominal, Min, Max และข้อมูลเชื่อมต่อเซนเซอร์
- **กำหนด Active Product**: คลิกปุ่มเครื่องหมายถูกสีเขียว (Select Active) ที่รายชื่อสินค้า เพื่อกำหนดให้เป็นสินค้าที่จะใช้สำหรับการตรวจสอบงานในปัจจุบัน
- **ลบสินค้า**: คลิกไอคอนถังขยะเพื่อลบสินค้านั้นๆ

---

## 4. แก้ไขข้อมูลสินค้าปัจจุบัน (Active Product Edit)
*สิทธิ์การเข้าถึง: Admin, Supervisor*
- หน้านี้เป็นทางลัดสำหรับแก้ไขตั้งค่า Spec Points ของสินค้าที่กำลัง **Active** อยู่ในขณะนั้น เพื่อให้สามารถปรับจูนค่า Nominal, Min, Max หรือเกณฑ์ควบคุม (Ctrl Min/Max) ได้อย่างรวดเร็ว

---

## 5. ตั้งค่าเซนเซอร์ (Sensor Settings)
*สิทธิ์การเข้าถึง: Admin, Supervisor*
- ทางฝั่งซอฟต์แวร์สามารถตั้งค่าและจัดการเซนเซอร์ที่เชื่อมต่ออยู่
- มีแท็บแบ่งระหว่าง **Mitutoyo** และ **Airgauge**

---

## 6. กระบวนการวัดและตรวจสอบคุณภาพ (Measurement Process)
*สิทธิ์การเข้าถึง: Admin, Supervisor, Operator*
กระบวนการนี้แบ่งเป็น 3 ขั้นตอนหลักต่อเนื่องกัน (Step-by-step) ดังนี้:

### Step 1: ตรวจสอบบาร์โค้ด (Barcode Check)
- ผู้ปฏิบัติงานต้องสแกนบาร์โค้ด 2 ฝั่ง (First Barcode และ Second Barcode) 
- ระบบจะตรวจสอบว่าบาร์โค้ดทั้งสองตรงกันหรือไม่ 
  - หาก **ตรงกัน (OK)** จะเข้าสู่ขั้นตอนถัดไป 
  - หาก **ไม่ตรงกัน (NG)** ระบบจะแจ้งเตือนและให้เลือกว่าจะบันทึกเป็น NG หรือทำการสแกนใหม่

### Step 2: ตรวจสอบด้วย Mitutoyo (Quality Check - Mitutoyo)
- หาก Product ที่ใช้งานอยู่มีการระบุให้ใช้ Mitutoyo ระบบจะแสดงจุดที่ต้องวัดทั้งหมด
- รอรับค่าการวัดจากเซนเซอร์ หากค่าที่วัดได้อยู่ในเกณฑ์ จะถือว่า PASS และส่งข้อมูลไปขั้นตอนถัดไป

### Step 3: ตรวจสอบระดับละเอียด (Quality Check - Air Gauge)
- หน้าจอจะแสดงหน้าปัดหรือตัวแสดงค่าแบบ Air Gauge
- ระบบจะรับค่าการวัด และประเมินร่วมกับ Spec Points ว่าผ่านเกณฑ์ (PASS) หรือไม่
- เมื่อทำการวัดครบและกด Save ระบบจะบันทึกข้อมูลการวัด (Measurement Data) อย่างสมบูรณ์

*ระหว่างกระบวนการ หากต้องการยกเลิก สามารถคลิกปุ่ม **Clear tmp** หรือ **Cancel Draft** ได้*

---

## 7. ประวัติการวัด (Measurement Dashboard / History)
*สิทธิ์การเข้าถึง: Admin, Supervisor, Operator*
หน้านี้ใช้สำหรับดูประวัติการตรวจสอบคุณภาพสินค้าที่ผ่านมา
- **ตารางข้อมูล**: แสดงข้อมูล ID, บาร์โค้ด (Serial A/B), ชื่อสินค้า, วันที่ และผลลัพธ์สุดท้าย (OK/NG)
- **การค้นหาและกรองข้อมูล**: สามารถพิมพ์ค้นหาจาก Serial Number, กรองเฉพาะผล OK/NG หรือกรองตามช่วงวันที่ (Start Date - End Date) ได้
- **ดูรายละเอียด (View Details)**: คลิกไอคอนรายละเอียดเพื่อดูผลการวัดของแต่ละ Spec Point ภายในรายการนั้นๆ เพื่อวิเคราะห์ว่าจุดใดผ่านหรือไม่ผ่าน
- **Export CSV**: คลิกปุ่ม "Export CSV" รายงานจะถูกดาวน์โหลดเป็นไฟล์ .csv สำหรับนำไปวิเคราะห์ใน Excel ต่อไป